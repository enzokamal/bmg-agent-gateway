apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agent.secret.name" . }}
  namespace: {{ .Values.global.namespace | default "default" }}
type: Opaque
stringData:
  DEEPSEEK_API_KEY: {{ include "agent.deepseekApiKey" . }}