apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.deployment.name }}
  namespace: {{ .Values.global.namespace | default "default" }}
  labels:
    {{- include "mcp-mssql.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.deployment.replicas }}
  selector:
    matchLabels:
      {{- include "mcp-mssql.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mcp-mssql.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: mcp-mssql
          image: {{ include "mcp-mssql.image.repository" . }}:{{ include "mcp-mssql.image.tag" . }}
          imagePullPolicy: {{ include "mcp-mssql.image.pullPolicy" . }}
          env:
            - name: MSSQL_SERVER
              value: {{ .Values.deployment.env.mssqlServer | quote }}
            - name: MSSQL_DATABASE
              value: {{ .Values.deployment.env.mssqlDatabase | quote }}
            - name: MSSQL_PORT
              value: {{ .Values.deployment.env.mssqlPort | quote }}
            - name: MSSQL_USER
              value: {{ .Values.deployment.env.mssqlUser | quote }}
            - name: MSSQL_PASSWORD
              value: {{ .Values.deployment.env.mssqlPassword | quote }}