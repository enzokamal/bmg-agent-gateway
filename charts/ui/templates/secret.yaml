apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ui.secret.name" . }}
  namespace: {{ .Values.global.namespace | default "default" }}
type: Opaque
stringData:
  AZURE_CLIENT_SECRET: {{ include "ui.azureClientSecret" . }}
  SECRET_KEY: {{ include "ui.secretKey" . }}